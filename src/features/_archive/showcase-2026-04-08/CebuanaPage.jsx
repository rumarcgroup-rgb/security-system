import CebuanaUploadPreviewPage from "./CebuanaUploadPreviewPage";

export default function CebuanaPage({ profile = null }) {
  return <CebuanaUploadPreviewPage profile={profile} mode="production" />;
}
